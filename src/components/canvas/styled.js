import styled from 'styled-components'

const Wrapper = styled.div`
  position: absolute;
  width: 100%;
  height: 100%;
  top: 0;
  left: 0;

  canvas {
    width: 100%;
    height: 100%;
  }
`

export { Wrapper }