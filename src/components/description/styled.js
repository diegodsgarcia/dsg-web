import styled from 'styled-components'

const Description = styled.div`
  .title {
    font-size: 1.2rem;
    color: var(--title);
    margin: 1rem 0;
  }

  ul, p {
    color: var(--text);
  }
`

export { Description };